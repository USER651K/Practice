*   VALIDATION: Check ZZPORT constraints (LOG ONLY - NO INTERRUPTION)
IF lv_zzport IS INITIAL.
* Log: Port not found - but continue processing
  ls_log-type = 'W'.
  ls_log-message = |Port not found for: { lv_pod_desc } - continuing with blank port|.
  ls_log-vbeln = ls_vbak-vbeln.
  APPEND ls_log TO lt_log.
  " Continue processing with blank port
ELSE.
*   Check up to 10 characters constraint
  DATA: lv_port_length TYPE i.
  lv_port_length = strlen( lv_zzport ).

  IF lv_port_length GT 10.
*   Log: Port exceeds limit - but continue processing
    ls_log-type = 'W'.
    ls_log-message = |Port { lv_zzport } exceeds 10 character limit (Length: { lv_port_length }) - using as-is|.
    ls_log-vbeln = ls_vbak-vbeln.
    APPEND ls_log TO lt_log.
    " Continue with the port value as-is
  ENDIF.

*   Check alpha-numeric characters only
  IF lv_zzport CN 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'.
*   Log: Invalid characters - but continue processing  
    ls_log-type = 'W'.
    ls_log-message = |Port { lv_zzport } contains non-alpha-numeric characters - using as-is|.
    ls_log-vbeln = ls_vbak-vbeln.
    APPEND ls_log TO lt_log.
    " Continue with the port value as-is
  ENDIF.
ENDIF.

* Always proceed with sales order creation (regardless of validation results)
ls_vbakkoz-zzport = lv_zzport.      " Use whatever port value we have (could be blank)
ls_vbak_bape-zzport = lv_zzport.    " Use whatever port value we have (could be blank)
